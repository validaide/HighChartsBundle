name: Build & Test Library
on: [ push ]
jobs:
  build:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        operating-system: [ ubuntu-22.04 ]
        php-versions: [ '8.1' ]
    name: PHP ${{ matrix.php-versions }} Test on ${{ matrix.operating-system }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-versions }}
          tools: composer,phpstan
        env:
          COMPOSER_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install PHP Dependencies
        run: composer install --optimize-autoloader --prefer-dist
        env:
          COMPOSER_AUTH: '{"github-oauth": {"github.com": "${{secrets.COMPOSER_GITHUB_ACTIONS }}"} }'
          SYMFONY_ENV: test

      - name: Puppeteer install
        run: npm install puppeteer -g

      - name: Puppeteer configuration
        run: puppeteer browsers install chrome@121.0.6167.85

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm' # Optional: caches node_modules based on lockfile

      - name: Highcharts install
        run: npm install highcharts-export-server@3.1.1 -g
        env:
          ACCEPT_HIGHCHARTS_LICENSE: 1

      - name: Unit Tests
        run: vendor/bin/phpunit

      - name: Static Analysis (phpstan)
        run: phpstan analyse --memory-limit=-1 --no-progress
